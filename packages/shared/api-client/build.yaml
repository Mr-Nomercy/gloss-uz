targets:
  $default:
    builders:
      retrofit_generator:
        options:
          named_parameters: true
      json_serializable:
        options:
          field_rename: snake
          explicit_to_json: true
